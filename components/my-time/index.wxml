<block wx:if="{{true}}">
  <my-input props="{{props}}" value="{{label}}"></my-input>
  <picker class="select-picker" mode="time" value="{{value}}" bindchange="onChange" start="{{props.start}}" end="{{props.end}}">
    <view class="select-picker-box">
      <view class="select-picker-btn">选择</view>
    </view>
  </picker>
</block>